version: 2
updates:
  # Keep workflow action versions current, rather than hand-pinning from memory.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
