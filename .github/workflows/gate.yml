name: shiplock gate

# Reusable release gate, consumed by any repo via:
#
#   jobs:
#     gate:
#       uses: shehuphd/shiplock/.github/workflows/gate.yml@main
#       with:
#         shiplock-spec: "shiplock"   # "." for shiplock's own repo
#       secrets:
#         ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
#
# Layer 1 (check) runs the deterministic checks. Layer 2 (audit) runs the
# semantic audit through Claude Code and opens an issue on failure.

on:
  workflow_call:
    inputs:
      python-version:
        description: "Python version the checks run on"
        type: string
        default: "3.12"
      shiplock-spec:
        description: "pip requirement for shiplock itself (use '.' in shiplock's own repo)"
        type: string
        default: "shiplock"
      run-audit:
        description: "Run the Layer 2 semantic audit"
        type: boolean
        default: true
      audit-runner:
        description: "Agent CLI that runs the audit: claude | codex"
        type: string
        default: "claude"
      audit-model:
        description: "Model for the audit run, in the declared runner's own naming"
        type: string
        default: "sonnet"
      audit-fallback-runner:
        description: >-
          Agent CLI for the retry when the first attempt dies mid-run: claude |
          codex. Empty disables failover. Declare the runner whose provider the
          fallback key belongs to — the whole point is a second, independent
          provider or billing account, since credit exhaustion is
          account-level and a sibling key from the same account is just as
          empty.
        type: string
        default: ""
      audit-fallback-model:
        description: "Model for the fallback attempt, in the fallback runner's naming"
        type: string
        default: ""
      audit-permission-mode:
        description: "Claude Code permission mode for the read-only audit run (claude runner only)"
        type: string
        default: "dontAsk"
    secrets:
      AUDIT_API_KEY:
        description: >-
          API key for the declared audit-runner's provider; required only when
          run-audit is true.
        required: false
      AUDIT_FALLBACK_API_KEY:
        description: "API key for the declared audit-fallback-runner's provider."
        required: false

# Least privilege: no scopes at the top level; each job names what it needs.
permissions: {}

jobs:
  check:
    name: Deterministic checks
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          # Tags are needed for the versioned-files check to diff against the
          # last reachable tag.
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install shiplock and the repo
        run: |
          python -m pip install --upgrade pip
          python -m pip install "${{ inputs.shiplock-spec }}"
          # The repo itself must be importable for the version and coverage checks.
          python -m pip install .
      - name: Run shiplock check
        run: shiplock check

  audit:
    name: Semantic audit
    needs: check
    if: ${{ inputs.run-audit }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ inputs.python-version }}
      - name: Install shiplock
        run: |
          python -m pip install --upgrade pip
          python -m pip install "${{ inputs.shiplock-spec }}"
          python -m pip install .
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - name: Install the audit runners
        run: |
          set -euo pipefail
          install_runner() {
            case "$1" in
              claude) npm install -g @anthropic-ai/claude-code ;;
              codex)  npm install -g @openai/codex ;;
              "")     ;;
              *) echo "::error::unknown audit runner '$1'; the gate knows claude and codex" >&2; exit 1 ;;
            esac
          }
          install_runner "${{ inputs.audit-runner }}"
          if [ "${{ inputs.audit-fallback-runner }}" != "${{ inputs.audit-runner }}" ]; then
            install_runner "${{ inputs.audit-fallback-runner }}"
          fi
          # A cross-runner fallback can't inherit the primary's model name —
          # model naming is per provider. Fail here, not mid-failover.
          if [ -n "${{ inputs.audit-fallback-runner }}" ] \
            && [ "${{ inputs.audit-fallback-runner }}" != "${{ inputs.audit-runner }}" ] \
            && [ -z "${{ inputs.audit-fallback-model }}" ]; then
            echo "::error::audit-fallback-runner is a different runner than audit-runner, so audit-fallback-model must name a model in that runner's own naming" >&2
            exit 1
          fi
      - name: Run the semantic audit
        id: audit
        env:
          PRIMARY_KEY: ${{ secrets.AUDIT_API_KEY }}
          FALLBACK_KEY: ${{ secrets.AUDIT_FALLBACK_API_KEY }}
        run: |
          set -euo pipefail
          shiplock prompt > audit-prompt.md
          # One adapter per runner: read-only tools, JSON-ish output captured
          # to the named file, the runner's own key variable set from the
          # neutral secret. The prompt's progress log (audit-progress.md) is
          # the state a second attempt continues from — it works across
          # runners, unlike any one provider's session store.
          run_attempt() {  # $1 runner, $2 model, $3 key, $4 prompt-file, $5 out-file
            SID="$(python3 -c 'import uuid; print(uuid.uuid4())')"
            case "$1" in
              claude)
                ANTHROPIC_API_KEY="$3" claude -p "$(cat "$4")" \
                  --session-id "$SID" \
                  --model "$2" \
                  --allowedTools "Read,Grep,Glob,Write(audit-progress.md)" \
                  --permission-mode "${{ inputs.audit-permission-mode }}" \
                  --output-format json > "$5"
                ;;
              codex)
                # workspace-write so the progress log can be written; approvals
                # off for CI. --json streams events whose turn.completed line
                # carries token usage; the final message goes to its own file.
                CODEX_API_KEY="$3" OPENAI_API_KEY="$3" codex exec \
                  --model "$2" \
                  --sandbox workspace-write \
                  --ask-for-approval never \
                  --json \
                  --output-last-message "$5.txt" \
                  - < "$4" > "$5.events"
                python3 -c '
          import json, sys, pathlib
          base = sys.argv[1]
          result = pathlib.Path(base + ".txt").read_text()
          usage = {}
          for line in pathlib.Path(base + ".events").read_text().splitlines():
              try:
                  event = json.loads(line)
              except ValueError:
                  continue
              if event.get("type") == "turn.completed":
                  usage = event.get("usage") or {}
          mapped = {
              "input_tokens": usage.get("input_tokens"),
              "output_tokens": usage.get("output_tokens"),
              "cache_read_input_tokens": usage.get("cached_input_tokens"),
          }
          envelope = {"result": result,
                      "usage": {k: v for k, v in mapped.items() if v is not None}}
          print(json.dumps(envelope))' "$5" > "$5"
                ;;
              *) echo "::error::unknown audit runner '$1'" >&2; return 64 ;;
            esac
          }
          rc=0
          run_attempt "${{ inputs.audit-runner }}" "${{ inputs.audit-model }}" \
            "$PRIMARY_KEY" audit-prompt.md audit.json || rc=$?
          if [ "$rc" -ne 0 ] && [ -n "${{ inputs.audit-fallback-runner }}" ] && [ -n "${FALLBACK_KEY:-}" ]; then
            echo "first attempt (${{ inputs.audit-runner }}) exited $rc; continuing on ${{ inputs.audit-fallback-runner }}" >&2
            mv audit.json audit-first.json 2>/dev/null || true
            {
              echo "The previous run of this audit was interrupted mid-way by an API failure."
              echo "Its progress log, if one exists at audit-progress.md, records the questions already settled — continue from the first question it doesn't cover, per the instructions below."
              echo ""
              cat audit-prompt.md
            } > audit-resume-prompt.md
            fb_model="${{ inputs.audit-fallback-model }}"
            rc=0
            run_attempt "${{ inputs.audit-fallback-runner }}" "${fb_model:-${{ inputs.audit-model }}}" \
              "$FALLBACK_KEY" audit-resume-prompt.md audit.json || rc=$?
          fi
          exit "$rc"
      - name: Act on the verdict
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -uo pipefail
          if [ ! -s audit.json ]; then
            echo "::error::the audit produced no output" >&2
            exit 1
          fi

          # Usage report first, and fail-soft: a missing field prints n/a and
          # can never block the verdict. The cost figure is the CLI's own
          # estimate; a tokens-to-USD conversion via the rates package is a
          # planned follow-up.
          usage_row() { jq -r "$2 // \"n/a\"" "$1" 2>/dev/null || echo "n/a"; }
          table_row() {
            echo "| $2 | $(usage_row "$1" '.usage.input_tokens') | $(usage_row "$1" '.usage.output_tokens') | $(usage_row "$1" '.usage.cache_read_input_tokens') | $(usage_row "$1" '.usage.cache_creation_input_tokens') | $(usage_row "$1" '.total_cost_usd') |"
          }
          {
            echo "### Semantic audit usage"
            echo ""
            echo "| Attempt | Input | Output | Cache read | Cache write | Cost (CLI estimate, USD) |"
            echo "|---|---|---|---|---|---|"
            if [ -s audit-first.json ]; then
              table_row audit-first.json "1 (interrupted, ${{ inputs.audit-runner }})"
              table_row audit.json "2 (continued, ${{ inputs.audit-fallback-runner }})"
            else
              table_row audit.json "1 (${{ inputs.audit-runner }})"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          in_tok="$(usage_row audit.json '.usage.input_tokens')"
          out_tok="$(usage_row audit.json '.usage.output_tokens')"
          cache_r="$(usage_row audit.json '.usage.cache_read_input_tokens')"
          cache_w="$(usage_row audit.json '.usage.cache_creation_input_tokens')"
          cost="$(usage_row audit.json '.total_cost_usd')"
          usage_line="Audit usage: $in_tok in / $out_tok out (cache: $cache_r read, $cache_w written); CLI cost estimate: $cost USD."
          if [ -s audit-first.json ]; then
            usage_line="$usage_line Continued on the fallback runner after an interrupted first attempt; both attempts are in the job summary."
          fi

          jq -r '.result // empty' audit.json > audit-output.txt 2>/dev/null || true
          if [ ! -s audit-output.txt ]; then
            echo "::error::the audit result carried no text" >&2
            exit 1
          fi
          verdict="$(grep -E '^AUDIT: (PASS|FAIL)' audit-output.txt | tail -1 || true)"
          if echo "$verdict" | grep -q 'AUDIT: FAIL'; then
            printf '\n\n---\n%s\n' "$usage_line" >> audit-output.txt
            gh issue create \
              --title "Shiplock audit failed on ${GITHUB_SHA::7}" \
              --body-file audit-output.txt
            echo "::error::semantic audit failed; opened an issue" >&2
            exit 1
          elif echo "$verdict" | grep -q 'AUDIT: PASS'; then
            echo "semantic audit passed — $usage_line"
          else
            # No verdict line is not a pass. Fail closed.
            echo "::error::no AUDIT verdict line found in the audit output" >&2
            exit 1
          fi
